{
  "name": "nyaovim-treeview",
  "version": "0.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "browserify src/main.jsx -o index.js -d -t babelify",
    "watch": "chokidar 'src/**/*.(jsx|js)' -c 'npm run build && echo ok'",
    "serve": "cd \"$(npm prefix)/../..\" && python -m SimpleHTTPServer 1234",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "react": "^0.14.7",
    "react-dom": "^0.14.7",
    "react-treeview": "^0.4.3"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "chokidar-cli": "^1.2.0"
  },
  "babel": {
    "presets": [
      "react",
      "es2015"
    ]
  }
}
